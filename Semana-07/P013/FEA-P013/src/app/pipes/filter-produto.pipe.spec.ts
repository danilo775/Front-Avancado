import { FilterProdutoPipe } from './filter-produto.pipe';

describe('FilterProdutoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProdutoPipe();
    expect(pipe).toBeTruthy();
  });
});
